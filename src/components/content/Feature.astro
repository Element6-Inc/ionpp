---
import { Image } from "astro:assets";

interface Props {
  title: string;
  description: string;
  buttonText?: string;
  buttonLink?: string;
  buttonTarget?: string;
  imageSrc: ImageMetadata;
  imageAlt: string;
  imagePosition: "left" | "right";
}

const {
  title,
  description,
  buttonText,
  buttonLink,
  buttonTarget,
  imageSrc,
  imageAlt,
  imagePosition,
} = Astro.props;

const isImageLeft = imagePosition === "left";
const showButton = buttonText && buttonLink;
---

<div class="overflow-hidden py-10 sm:py-14">
  <div class="mx-auto max-w-7xl px-6 md:px-10 lg:px-16">
    <div
      class="grid grid-cols-1 gap-x-20 gap-y-10 sm:gap-y-24 lg:grid-cols-2 lg:items-center"
    >
      <!-- Image Section -->
      <div
        class={`lg:px-0 ${isImageLeft ? "order-1 md:order-1" : "order-1 md:order-2"}`}
      >
        <div class="overflow-hidden md:rounded-tr-4xl md:rounded-bl-4xl">
          <Image
            src={imageSrc}
            alt={imageAlt}
            sizes="(max-width: 640px) 100vw, (max-width: 1024px) 50vw, 560px"
            loading="lazy"
            decoding="async"
            class="h-full w-full object-cover object-center"
          />
        </div>
      </div>

      <!-- Content Section -->
      <div
        class={`lg:px-0 ${isImageLeft ? "order-2 md:order-2" : "order-2 md:order-1"}`}
      >
        <div class="mx-auto max-w-2xl lg:mx-0 lg:max-w-xl">
          <h3>{title}</h3>
          <div class="mt-4" set:html={description} />

          {
            showButton && (
              <div class="mt-8">
                <a
                  href={buttonLink}
                  target={buttonTarget || "_self"}
                  rel={
                    buttonTarget === "_blank"
                      ? "noopener noreferrer"
                      : undefined
                  }
                  class="inline-block btn btn-secondary"
                >
                  {buttonText}
                </a>
              </div>
            )
          }
        </div>
      </div>
    </div>
  </div>
</div>
